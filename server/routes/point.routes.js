/* const router = require("express").Router();
const {
  savePointController,deletePointController
} = require("../controllers/point.controller");

router.post("/",  savePointController);
router.delete("/", deletePointController)
router.get('/', )


module.exports = router; */